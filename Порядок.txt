ПОРЯДОК ПРИМЕНЕНИЯ

1. Распаковать поверх ветки bugfix/issue-description.

2. УДАЛИТЬ ЧЕТЫРЕ ФАЙЛА (переехали в docs/archive/, zip не умеет удалять):

     git rm docs/specifications/02_POSTGRESQL_ARCHITECTURE_SPEC_v1.0_ORIGINAL.md \
            docs/specifications/06_FRONTEND_DELIVERY_ARCHITECTURE_v1.0_HISTORICAL.md \
            docs/specifications/Neon_Seed_Specification.md \
            docs/deployment/QWEN_DEPLOYMENT_SPEC.md

3. make dev-reset && make dev-test        -> ожидается 66 + 23 зелёных

4. git add . && git commit && git push     (в ветку bugfix)

5. ПЕРЕД мёржем в main:  bash scripts/reset-neon.sh
   Миграция 16 удаляет колонки dev_status_code/current_level.
   Старая схема с новым бэкендом НЕСОВМЕСТИМА -- порядок нарушать нельзя.

6. Мёрдж в main -> Render и Cloudflare деплоят автоматически.

ВНИМАНИЕ: compatibility_date в frontend/wrangler.jsonc выставлен на 2026-09-01.
Если выкладка будет в другой день -- поменяй на дату реального деплоя.

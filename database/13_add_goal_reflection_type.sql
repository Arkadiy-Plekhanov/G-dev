-- ============================================================
-- 13. reflection_type: добавлен 'goal'
-- ============================================================
-- Раньше рефлексия, привязанная к цели, вынужденно сохранялась как
-- 'weekly' -- значения 'goal' в option_lists просто не существовало.
-- Это не было багом логики фронтенда: подписи "Weekly reflection"
-- внутри карточки рефлексии, привязанной к цели, и на карточке самой
-- цели -- честное следствие того, что типа для этого случая не было
-- в доменной модели вообще (обратная связь с реального использования:
-- владелец увидел подпись "Weekly" под целью и справедливо удивился).
--
-- reflection_type_code -- составной FK на option_lists(list_type, code)
-- (см. 01_schema..., reflections_type_fk), поэтому добавление нового
-- допустимого значения -- ровно одна строка, без изменения структуры
-- самой таблицы reflections.
-- ============================================================

BEGIN;

INSERT INTO option_lists (list_type, code, label, sort_order) VALUES
    ('reflection_type', 'goal', 'По цели', 50);

COMMIT;
